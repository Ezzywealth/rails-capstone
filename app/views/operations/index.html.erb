<%= render partial: 'shared/nav', locals: {title:'Transactions', symbol:'<', url: categories_path}%>
<main class='operations_page'>
  <section>
  <h2 class='operation_category'>
    <p>
    <%= @category.name %>
    </p>
    <p class='category_amount'>
      $<%= @operations.sum {|item| item.operation[:amount].to_i} %>
    </p>
  </h2>

    <section class='operations'>
      <% @operations.each do |operation| %>
        <div class='operation'>
          <div class='operation_left'>
            <span class='operation_name'>
              <h4><%= operation.operation.name %></h4>
            </span>
          </div>
            <p class='operation_amount'>$<%= operation.operation.amount %></p>
              <p class='operation_date'><%= operation.created_at.strftime('%b %d, %Y') %></p>
        </div>
      <% end %>
    </section>
  </section>
  <button>
    <%= link_to 'Add New Transaction', new_category_operation_path, class:'button' %>
  </button>
</main>